apiVersion: v1
kind: Service
metadata:
  name: frontend-app-td-service
spec:
  type: LoadBalancer
  selector:
    app: frontend-td-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
